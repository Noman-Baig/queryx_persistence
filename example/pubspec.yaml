name: queryx_persistence_example
description: Runnable example of queryx_persistence (sqlite3 backend).
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  sqlite3: ^2.7.6
  queryx_persistence:
    path: ../

dev_dependencies:
  lints: ^4.0.0
